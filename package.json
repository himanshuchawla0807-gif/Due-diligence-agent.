{
  "name": "due-diligence-agent-open",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python cli.py",
    "dev:backend": "python -m uvicorn backend.main:app --host 0.0.0.0 --port 8102 --reload",
    "test": "python -m pytest tests",
    "install:python": "python -m pip install -r requirements.txt"
  },
  "dependencies": {},
  "devDependencies": {}
}
